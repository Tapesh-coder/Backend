<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
    <h1 class="text-white text-bolder text-3xl pl-[45%] ">Note Maker</h1>
    <div class="main">
        <div class="form text-white p-10" >
           <form action="./create" method="post">
            <input type="text"  class="bg-zinc-800 p-5 w-full rounded-lg outline-none" placeholder="Title" name="title">
            <textarea name="details" class="bg-zinc-800 p-5 w-full resize-none mt-3 rounded-lg outline-none" placeholder="Write your details here" id=""></textarea>
            <input type="submit" class="bg-blue-500 mt-3 p-3 rounded-lg" name="" id="">
           </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if ((files.length)>0) { %>
                <% files.forEach((val)=>{%>
<div class="task min-w-73 px-5 py-4 rounded-lg bg-zinc-800">
    <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
    <a href="/file/<%= val %>" class="text-blue-500 inline-block mt-3 ">read more</a>
</div>
              <%  })%>
            <%} else { %>
                <h3 class="text-zinc-500 text-xl">No taks yet</h3>
             <%} %>
            
           
           
           
        </div>
    </div>
</body> 
</html>